anthropic>=0.92.0
pydantic>=2.0
requests>=2.28
pyyaml>=6.0

# Used by check_resume_format.py to extract text from your real PDF/DOCX
# resume the way an ATS would (deliberately simple parsing — no fancy
# layout reconstruction).
PyPDF2>=3.0
python-docx>=1.0

# Optional: install `requirements-full.txt` or `pip install -e .[embeddings]`
# to enable domain_fit_score via sentence-transformers. The pipeline runs
# without it; domain_fit_score will be blank.
